# High Load Testing
docker run --rm httpd:2.4 ab -n 100000 -c 500 -k -r -s 600 -t 300 https://app.saqlainmustaq.com/

This command sets up the following test parameters:

-n 100000: 100,000 total requests
-c 500: 500 concurrent connections
-k: Use HTTP KeepAlive
-r: Don't exit on socket receive errors
-s 600: Set a 600-second (10 minute) timeout for socket operations
-t 300: Run the test for a maximum of 300 seconds (5 minutes)

# Normal Testing
docker run --rm httpd:2.4 ab -n 1000 -c 100 -f TLS1.3 -k -r -s 300 https://app.saqlainmushtaq.com/

This command does the following:

-n 1000: Send 1000 requests in total
-c 100: Use 100 concurrent connections
-f TLS1.3: Force TLS 1.3 protocol
-k: Use HTTP KeepAlive
-r: Don't exit on socket receive errors
-s 300: Set a 300-second timeout for socket operations

# High Load Testing
docker run --rm httpd:2.4 ab -n 1000000 -c 1000 -f TLS1.3 -k -r -s 1200 -t 600 https://app.saqlainmushtaq.com/

This command sets up the following test parameters:

-n 1000000: 1 million total requests
-c 1000: 1000 concurrent connections
-f TLS1.3: Force TLS 1.3 protocol
-k: Use HTTP KeepAlive
-r: Don't exit on socket receive errors
-s 1200: Set a 1200-second (20 minute) timeout for socket operations
-t 600: Run the test for a maximum of 600 seconds (10 minutes)
